{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python Debug Classifier",
      "type": "debugpy",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "env": {
        "PYTHONPATH": "${workspaceRoot}"
      }
    }
  ]
}
